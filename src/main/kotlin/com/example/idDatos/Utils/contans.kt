package com.example.idDatos.Utils

class contans {
    companion object {
        private const val URL = "/api"
        private const val URL_VER = "/v1"
        private const val URL_Base = URL + URL_VER
         const val URL_BASE = "$URL_Base/personas"


    }
}